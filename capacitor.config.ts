import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Epicollect5 Media Downloader',
  webDir: 'dist'
};

export default config;
